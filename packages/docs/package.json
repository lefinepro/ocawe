{
  "name": "cogni-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:playground": "cd ../playground && bun run build:docs",
    "dev": "vitepress dev .",
    "build": "bun run sync:playground && vitepress build .",
    "preview": "vitepress preview .",
    "sync:openapi": "node scripts/sync-openapi.mjs"
  },
  "dependencies": {
    "vitepress": "^1.6.3"
  }
}
