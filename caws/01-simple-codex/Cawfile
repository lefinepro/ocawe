# 01 - Simple Codex Agent
#
# Demonstrates the simplest possible coding agent setup.
# Runs codex agent with default model.

settings do
  data.adapter = "memory"
  port = 4111
end

@[Model("cliproxy")]
workflow "01-simple-codex" do
  agent "codex-agent"
end
